name: ai-sdk

on:
  push:
    branches: [main]
    paths:
      - 'ai-sdk/**'
      - '.github/workflows/test-ai-sdk.yaml'
  pull_request:
    branches: [main]
    paths:
      - 'ai-sdk/**'
      - '.github/workflows/test-ai-sdk.yaml'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        pkg: [acp, harness-pi, harness-codex, harness-claude-code]
    defaults:
      run:
        shell: bash -l {0}
        working-directory: ai-sdk/${{ matrix.pkg }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh
      # --prerelease=allow: benchflow pins an rc litellm
      - name: Install dependencies
        run: uv pip install --python ${Python_ROOT_DIR} --prerelease=allow -e '.[dev]'
      - name: node --check server.mjs
        run: node --check src/*/server.mjs
      - name: Run pytest
        run: pytest -q
      - name: Ruff
        run: uvx ruff check src tests
