{
  "name": "@benchflow/ai-sdk-agents",
  "private": true,
  "description": "Shared node deps for the ai-sdk/ agent harnesses, so the uniform offline wire-parity re-drive runs (one manifest covers every ai-sdk/<agent>/src/*/server.mjs via up-the-chain ESM resolution).",
  "dependencies": {
    "@ai-sdk/openai-compatible": "^2.0.50",
    "ai": "^6.0.204",
    "zod": "^4.4.3",
    "@ai-sdk/harness": "^1.0.6",
    "@ai-sdk/harness-pi": "^1.0.6",
    "@ai-sdk/sandbox-just-bash": "^1.0.6",
    "just-bash": "^3.0.2"
  }
}
